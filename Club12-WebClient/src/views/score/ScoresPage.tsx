import { Card, CardContent, Typography } from '@mui/material';

const ScoresPage: React.FC = () => {
  return (
    <Card>
      <CardContent>
        <Typography variant="h6">Puntuaciones</Typography>
      </CardContent>
    </Card>
  );
};

export default ScoresPage;
